/** Nombres de coleccion en un solo lugar para evitar cadenas sueltas. */
export const COLLECTIONS = {
  users: 'users',
  communities: 'communities',
  requests: 'requests',
  improvements: 'improvements',
} as const;

export const SESSION_COOKIE = 'argyra_session';
